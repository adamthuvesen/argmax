node_modules/
dist/
coverage/
release/
*.log
*.sqlite
*.sqlite-shm
*.sqlite-wal
.DS_Store
argmax-icons.local.json
.claude/
maestro/
COMPETITIVE_ANALYSIS.md

# Secrets. Nothing in the repo reads a .env today; this is here so one
# arriving later can't be staged by a `git add` that meant to catch a
# neighbouring file.
.env
.env.*

# Tauri / Rust port
src-tauri/target/
src-tauri/gen/

# Scratch files (e.g. pasted session transcripts)
tmp.txt
*.tmp
scratch/

# Workspace worktrees, when a project points its worktree location inside the repo
.argmax/
