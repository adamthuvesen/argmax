/** Smallest useful pane width for multi-session split views. */
export const SESSION_CELL_MIN_WIDTH_PX = 460;

/** Readable chat floor when a right-side review/log panel is docked. */
export const CHAT_PANE_MIN_WIDTH_PX = 520;
