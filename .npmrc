# Supply-chain hardening: only install package versions published >=1 day ago.
# Requires npm >=11.10.0 (Feb 2026). Silently ignored on older npm.
# Exclusion list (`min-release-age-exclude`) not yet shipped — track npm/cli#8994.
min-release-age=1
